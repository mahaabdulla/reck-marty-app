import 'package:flutter/material.dart';

class CharacterDetailsScreens extends StatelessWidget {
  const CharacterDetailsScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text('page 2'),),);
  }
}